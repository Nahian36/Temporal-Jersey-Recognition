# Temporal-Jersey-Recognition
MobileNetV3/EfficientNet-Lite0 frame encoder + bi-GRU attention over clips; predicts tens/ones separately. Training supports mixed precision, EMA, warmup-to-cosine LR, early stop; saves best weights/history. Eval script prints JSON metrics; sample dataset + CPU/GPU reqs.
